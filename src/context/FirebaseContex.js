import { createContext } from "react"; // Importa la función para crear un contexto en React.

// Crea un nuevo contexto de Firebase.
const firebaseContex = createContext();

// Exporta el contexto para ser utilizado en otras partes de la aplicación.
export default firebaseContex;
